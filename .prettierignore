dist/
.nx/
coverage/
node_modules/
*.gen.ts

# drizzle-kit writes these; reformatting them fights the generator on the next
# `drizzle-kit generate`, exactly as with *.gen.ts above.
apps/be-01/drizzle/**/snapshot.json

# Sibling checkouts of this same repo. Their contents belong to whatever commit
# that worktree has out, so formatting them here is both wrong and unstable.
.worktrees/

*.env.sops

# Vendored skill definitions — content-hashed in skills-lock.json, so
# reformatting them invalidates the hash and is undone on the next sync.
.agents/skills/
.claude/skills/

/.nx/workspace-data

# Symlinks to AGENTS.md, so the four agent CLIs each find the file they look
# for. Prettier exits 2 on an explicitly-listed symlink rather than following
# it, which fails `nx format:check`. AGENTS.md itself is still formatted.
/CLAUDE.md
/GEMINI.md
# Prettier 3.9.6 never settles on these four: a hard-wrapped **bold** paragraph
# inside a checklist item is indented one level deeper on every pass, without
# limit (measured 2026-09-06: twelve passes, still moving; the fourth joined
# on the merge of main the same day). They are change records, formatted by
# 3.8 as they stand; the day the bug is fixed, delete these lines and run
# `prettier --write` once.
openspec/changes/saved-plans/tasks.md
openspec/changes/dual-optimized-scheduler/tasks.md
openspec/changes/archive/2026-08-30-token-tracking/tasks.md
openspec/changes/work-item-deadline/tasks.md
