## 1. Explicit completed-operation contract

- [ ] 1.1 Start from landed R10 (#353, f66f73e8) and record the exact current run/refreshOrMarkStale/runMarkerWrite/stepStack callers in design.md's matrix. Preserve PlanRowReadings/PlanRenderRow, event-only PlanLiveValues.run, cellCards and attachCell signatures described there. Test the current behavior with `apps/fe-01/src/components/wbs/plan-read-and-write.test.tsx` and `src/lib/plan-refresh.test.ts`; baseline includes held reads, API-owner replacement and no socket.
- [ ] 1.2 Add LocalWrite.perform and RunPlanWrite in `src/lib/local-write.ts`; update use-plan-read and every run prop/caller to wrap each real API call in perform with conservative ALL_RESOURCES. Test `local-write.test.ts > keeps the completed prefix when a later request refuses`. Negative: add resources only after the entire gesture succeeds; at the point the second promise rejects assert the first operation's resources remain pending.
- [ ] 1.3 Integrate completed resources into the existing coordinator while preserving refusal, stale, busy and owner behavior. Production-page test `refreshes a created tag after its attachment refuses`: hold creation until after initial reads are counted, resolve it, refuse attachment, await directory answer then assert tag picker contains the created tag and row has no tag. Negative: drop prefix invalidation; no subscription is installed, so the named assertion must fail.

## 2. Narrow operation families

- [ ] 2.1 Narrow fields, estimates/actuals/progress/measures, row structure, dependencies, freeze/unfreeze and existing-person assignment to tree in `use-plan-fields.ts`, `use-estimate-drafts.ts`, `use-plan-structure.ts`, `use-plan-dependencies.ts`, `use-reference-sets.ts`, toolbar and cell callers. Test `plan-read-and-write.test.tsx > estimate refreshes only tree without a socket`; wait for changed server total, then assert exact read operations. Negative: restore ALL_RESOURCES for setEstimate; assertion must see extra directory/steps/markers. Negative: drop tree obligation; changed total must remain absent after the write answer.
- [ ] 2.2 Narrow step add/rename/remove to tree+steps and project settings/capacity/bands to tree. Tests use actual settings/step controls with counters started after initialization. Negative: omit steps after rename; await new tree then assert step heading uses the new name. Keep scope recovery conservative after a refused peer deletion.
- [ ] 2.3 Mark directory create/patch/delete and membership/kind calls tree+directory. For createTeamFor/createServiceFor/createTagFor/createTypeFor/createPersonFor, independently account for both requests. Parameterize success-prefix/refusal integration cases over all five functions. Negative per family: declare its create tree-only; after directory response window assert its newly created name is available without a socket echo.
- [ ] 2.4 Preserve marker-only invalidation, including modeled marker refusal, and preserve full undo/redo recovery. Run existing marker-deletion and stale-undo regressions. Negative: return before refused marker invalidation; after a peer deletion assert no deleted marker span. Do not use a retrying zero-count assertion during an intended silence window.

## 3. Cross-operation races and gates

- [ ] 3.1 In production-page tests overlap tree-only success with a pending directory operation and a pre-write tree GET. Assert each covering generation installs and the trailing tree read starts only after the held old one settles. Negative: reuse that old read to discharge the write; assert the later server value and the exact trailing request count.
- [ ] 3.2 Cover modeled refusal with no success, not_found, invalid-body refusal, ambiguous transport failure, and late completion from the previous project/API owner. Negative: apply an old owner's obligation to its replacement; observe replacement resource counters/toasts immediately at completion. Preserve typed client failure distinctions instead of string-catching all failures as a modeled refusal.
- [ ] 3.3 Run `bunx nx run fe-01:test`, `bunx nx run fe-01:typecheck` and `bunx nx run fe-01:lint`, then the full workspace gate (`bin/h2puni-gate.sh` on h2puni). Run the whole browser gate on owned ports because focus and toolbar/write sequencing changed, plus `OPENSPEC_TELEMETRY=0 openspec validate --all --json`. Write observed Proof comments and verify.md from actual output only.

All negative outcomes above are planned; no runtime verification occurred in the design pass.
