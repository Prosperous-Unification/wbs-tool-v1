## 1. Affected memberships (TDD)

- [x] 1.1 In the isolated worktree, promote this draft into the live OpenSpec change and read current Presence callers/tests plus R4/R9 ownership updates.
- [x] 1.2 Add failing affected-project tests for join/rejoin, old/new move, unchanged subscribe, stale/current unsubscribe, unknown id and disconnect; retain random index differential test and deduplicated tabs.
- [x] 1.3 Implement mutation return values and coherent index cleanup; preserve prior-project lookup before deletion and explicit no-op outcomes.

## 2. Scoped delivery (TDD)

- [x] 2.1 Add immediate production mutation/delivery count checks for 1,000 connections in 100 projects plus an unprojected newcomer; observe baseline global delivery of 1,001 frames.
- [x] 2.2 Add literal-recipient count cases for move, unsubscribe reset, same-project no-op, renamed-id rejoin and disconnect with deduplicated names.
- [x] 2.3 Implement indexed affected-project delivery and explicit initial/reset delivery; serialize each affected roster once and preserve controlled socket send failures.
- [x] 2.4 Inject global delivery into the production operation; observe unrelated-frame assertions fail before adding an adjacent Proof comment. Restore, then inject omitted old-project notification/reset as required to prove their checks.

## 3. Gateway wiring (TDD)

- [x] 3.1 Add production socket regressions with an old-project observer, new-project observer and unrelated project; inspect actual sends or read counts once after positive ping barriers, never a retrying absence matcher.
- [x] 3.2 Wire join, enter, unsubscribe and close to affected-project delivery plus initial/reset rosters, preserving awaited authentication and close ordering.
- [x] 3.3 Run existing project-content isolation and both verification-race cases; inject global fan-out on the app-called delivery path and record the observed failure.

## 4. Held verification

- [x] 4.1 Restore every injected fault, run explicit ./ gateway/contracts directory tests with loopback permission, source+spec typecheck, scoped lint and formatting, and strict OpenSpec validation.
- [x] 4.2 Record exact commands, totals, observed fault table and skipped checks in verify.md; hold for independent review without commits or broad gates during concurrent edits.
