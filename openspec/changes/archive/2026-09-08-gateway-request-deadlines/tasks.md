## 0. Ownership and promotion

- [x] 0.1 After parent gate release, promote the ignored draft through OpenSpec; recheck backend ownership, record R7 gateway hold and Wave2 runtime owner. Choose explicit production budget values and capability staging/final path in design before source edits. Test structural validation and actual composition typecheck after implementation; no new interview.

## 1. Backend request deadline

- [x] 1.1 TDD portable deadline/timer capability and injected backend PushClient options. Test invalid/nonfinite budgets/retry counts, deterministic now/timer control, success cleanup and already-aborted caller. Negative: remove budget validation or timer disposal and observe the actual production client call fail. Establish final runtime-portable boundary or explicit temporary backend adapter placement without contracts/core runtime import.
- [x] 1.2 TDD hung headers through production PushClient with maxRetries0, attempt1000ms/overall250ms. Advance literal249 then1: pending before, terminal at250, transport signal abort observed, no additional call after another1000. Negative omit signal propagation; failure must concern cancellation, not merely timer settlement.
- [x] 1.3 TDD stalled success JSON body and permanent-error diagnostic text separately. Deliver headers before expiry and hold body bytes; require body cancellation at deadline. Negative remove active abort propagation, watched separately for success and diagnostic bodies. Body consumption stays inside the one attempt callback, so the helper has no separate header-complete disposal hook. Restore and rerun scoped tests before Proof comments.
- [x] 1.4 TDD attempt/overall retry/backoff accounting: attempt400, overall1000, backoff500 leads first expiry400, second admission900, terminal1000. Negative remove overall clipping and observe second attempt outlive1000; no later attempt after cancellation. Test abort during backoff and backoff that cannot fit. Preserve retry-count and transient status policies.
- [x] 1.5 TDD modeled transport-network retry classification and unknown failure propagation. Observe actual platform network error representation in a bounded probe/test; classify at transport adapter. Tests transient rejection within budget, permanent4xx no retry, unexpected sentinel/programmer exception and malformed trusted body no retry. Negative broaden catch to retry unknown or suppress malformed response and watch exact call/error assertion fail.
- [x] 1.6 Prove actual fetch cancellation with a real local Bun HTTP server: hung headers, then headers plus stalled streaming body. Assert caller termination and server/stream cancellation or equivalent transport closure evidence, clean up listeners/server deterministically. Negative omit active fetch signal; wrapper settlement alone cannot satisfy oracle. Record platform-specific observations and any unavailable proof explicitly.

## 2. Backend composition and committed-edit preservation

- [x] 2.1 Wire explicit production transport/timers/budgets through services/boot without hidden extracted-core globals. Test actual composition is typechecked with required runtime/budget inputs; negative omit overallMs from services.ts and observe the source+spec typecheck reject that production caller. Deterministic transport tests separately prove the configured budget is enforced.
- [x] 2.2 Add broadcaster durability test with real PushClient and held transport: durable sequence exists while push pending and another write-lock turn enters; deadline reports delivery failure and publish settles without refusing the committed edit. Negatives move delivery under lock, rethrow delivery failure, or omit cancellation; each observed in its own window. Preserve existing rollback/journal/replay and post-batch publication tests.
- [x] 2.3 Backend scoped verification and independent review checkpoint. Run push/deadline/real-transport/broadcaster/durability/plan-command suites, scoped lint/format and parent-coordinated backend source+spec typecheck. Record test counts and all fault restorations. Hold backend files for parent review before gateway edits.

## 3. Gateway — only after R7 handoff

- [x] 3.1 Finalize shared runtime-portable placement if staged; TDD ForwardClient and extracted resume client with identical attempt/overall/body/caller cancellation capability. Preserve one forward attempt unless explicitly justified idempotent policy. Tests hung headers/body and exact overall termination on both production clients; negative omitted signal/body lifetime and no later attempts.
- [x] 3.2 TDD resume response status validation:503 carrying valid success-shaped JSON maps unavailable, never successful replay. Negative remove res.ok check. Malformed trusted body must stay an explicit error, not become a default empty success.
- [x] 3.3 Wire gateway construction and connection-owned cancellation after R7 lifecycle review. Real-socket tests send forward/resume through buildApp with stalled transport: live connection sees existing unavailable/refusal+ack then can ping; closed connection aborts pending request with no late send/failure metric. Negative omit close abort or late-send prevention; inspect absence at the relevant instant. Preserve R4 ingress and R7 join/leave regressions.
- [x] 3.4 Gateway scoped verification and review: clients/controller/ingress/presence/health tests, lint/format and parent source+spec typecheck. Verify health's existing independent timeout did not change. No broad gates while other owners edit.

## 4. Completion

- [x] 4.1 Parent frozen full gates, structural OpenSpec verification, fresh restoration results, review and commit. Every new check has actual failing fault output and adjacent Proof derived from it. Record no worker introduced, production budget values, genuine transport observations and any remaining limitation; no completion claim from finite attempt count alone.
