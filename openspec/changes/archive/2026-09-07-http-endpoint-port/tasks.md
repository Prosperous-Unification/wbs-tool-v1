## 0. Promotion and evidence window

- [x] 0.1 Promoted via OpenSpec after the freeze and source-backed collision/timing records. R1/R3/R5 integrated; no canonical specs found by CLI. Complete structural validation and record final owned-file inventory before closing.
- [x] 0.2 Record fresh be-01 and fe-01 source+spec typecheck wall-time baselines with commands, host, cache policy and samples; run again after foundation and full migration. Test: actual included type-negative fixture is rejected by the chosen target, not an empty solution compile. If time doubles, simplify PrincipalOf to the approved nullable principal/runtime check while retaining ParamsOf.
- [x] 0.3 Complete the source-backed request/refusal/status/operation inventory, including all command arms, detail payloads, OIDC mode routes and 501 unsupported_body_version. Pin existing MCP operation names. Test: inventory coverage against actual route composition and parser/service outcome fixtures, never committed OpenAPI alone. Update the normative status sketch under parent ownership after freeze.

## 1. Typed shape and adapter foundations

- [x] 1.1 TDD SchemaShape and declaration conversion in contracts. Tests: nested inline commands/optional fields, strict nested request extras, tolerant nested reply extras, known field type rejection, async validation, unsupported predicate and unvalidated morph output. Negatives: shallow reject, delete/strip request extras, dropped descriptor field/union arm, unsupported conversion returning an unconstrained schema. Document direction explicitly; keep initial declarations free of unmodeled transforms/defaults.
- [x] 1.2 TDD literal EndpointShape/ParamsOf/RequestPolicy, closed RefusalDetail and status-specific replies, then be-01 Endpoint/bind/EndpointInput/IdentityResolver. Type negatives on actual binding: wrong params key, mismatching params schema keys, successful refusal reply, read principal without identity, 429 body at 400, undeclared empty/text/redirect arm. Observe each exact tsc diagnostic; inject widening so @ts-expect-error becomes unused. Test EMPTY versus JSON null without casts.
- [x] 1.3 TDD adapter onRequest policy matrix from design.md before any production route migration. Tests through app.handle: identity variants, unsafe foreign-origin cookie project write absent, login/register always-origin, internal identity, malformed read-token write gives preparse403. Negatives: remove identity/origin or move policies after parsing; record actual parser fault status (preflight400, not guessed422). Preserve R3 valid token + thrown store failure as unexpected server failure.
- [x] 1.4 TDD reply rendering and validation: success/refusal schema/status pairs, 204 EMPTY, 200 JSON null, 302 Location, getSetCookie length3, TextResponse, 429 throttle, 503 snapshot_busy/dependency health, 501 unsupported_body_version details. Negatives: collapse null to empty, overwrite cookie headers, mismatch status/body or bypass either response arm validator. Direct handler calls remain framework-free.
- [x] 1.5 Establish descriptor emitter/MCP fixture before broad migration: inline object bodies, nested command anyOf arms and required/optional fields, explicit unique operationId. Negatives: remove operationId, nested property or union arm; use unsupported conversion fallback. Keep generic emitter free of validator introspection/backend imports.

Foundation completion is limited to the reviewed declarations, adapter and MCP fixture; see verify.md’s foundation checkpoint and fault records. It does not complete production route policy coverage or migration.

## 2. Incremental production endpoint migration

Each checkbox includes RED → minimal implementation → focused GREEN → named fault → restoration → fresh GREEN, one direct endpoint.handle(literal) test for each modeled refusal path, and a named wire-assertion rewrite entry in verify.md. Reuse existing \*.routes.ts modules and existing test filenames.

- [x] 2.1 Migrate smoke.routes; test smoke.integration plus direct endpoint refusal; negative deep unknown body field/invalid text and envelope mapping. Record freeform validation-message replacement.
- [x] 2.2 Migrate step.routes; test step.controller.db including in-use/cascade, identity and name parsing; negative malformed/unknown body fields refused before mutation, preserve in_use detail/status.
- [x] 2.3 Migrate work-item.routes including plan/directory commands and undo/redo; test work-item.controller, undo.controller.db and directory.controller.db. Negatives: strip derived number, nested unknown command property, wrong union arm, bad command ref, lost batch at/kind/detail; preserve all existing domain parsing and undo negatives.
- [x] 2.4 Migrate history.routes; test history.controller and direct not_found; negative lost query declaration/unknown-key rejection. Preserve optional kind/workItemId and unknown-kind empty result.
- [x] 2.5 Migrate solution.routes; test actual lookup endpoint and direct not_found; negative omitted path parameter/refusal mapping.
- [x] 2.6 Migrate saved-plan.routes; test saved-plan.controller.db including optional save name versus mandatory rename, compare queries, quota/corrupt details, 501 unsupported version and 503 snapshot_busy. Negatives: 503 or501 mapped to400, dropped version/detail fields, empty name silently defaulted, unknown field stripped.
- [x] 2.7 Migrate project.routes; test project.controller and relevant capacity/priority/scheduler cases. Negatives: origin removed from project POST, unknown patch key stripped, malformed nested weight/settings accepted, wrong enum/optional-null semantics. Preserve opened EMPTY and export representation/format refusal.
- [x] 2.7a Migrate newly integrated calendar-marker.routes after project: four shape operations and field-specific refusals, strict PATCH exactly-one writable field, UUID/name/date/color semantics and markerId MCP collision. Tests: calendar-marker.controller.db, calendar-marker-identity.db, marker repository/source exclusions, FE API/Gantt/zoned regressions. Negatives: rename wire markerId to id; accept unknown PATCH date; lose contrast field/status; let marker UUID enter work-item code; publish before successful store write. Retain marker-only refresh and no journal/revision/saved-plan inclusion.
- [x] 2.8 Migrate directory.routes six read shapes; test directory.controller.db and literal handlers. Negative missing identity policy and dropped array/entry response field; keep mutations owned by command shapes.
- [x] 2.9 Migrate internal.routes; test internal.integration and contracts internal schemas; negatives missing internal identity, invalid resume/forward fields and unchecked trusted response shape. Gateway protocol/runtime extraction remains out of scope.
- [x] 2.10 Migrate auth.routes only after R3/R5 are landed/owned. Test auth.integration and oidc.integration for password and OIDC compositions, 302/204/cookies and origin/scope policies. Retain R3 broad-catch fault and R5 held20 attempts/global-cap/release-after-throw faults on production paths; observe during pending verification, never only after completion. Preserve 429 invalid_credentials admission without converting it to401 or400. Carry fetched OIDC HEAD405+Allow/no-cookie/unspent-transaction and any-duplicate-key400 negatives before consume. Preserve raw arrived method and URL cardinality, error redirect allowlist/no description reflection, structured logging and narrow exchange401 catch. Preserve incoming d08a6ad6 live-state-mismatch record/cookie retention, missing/expired clearing, and forged-then-honest callback recovery with its derived cookie. Explicitly record old bodiless400/401 to Refusal envelope changes with identical public invalid-transaction bodies; account resolution remains outside exchange catch.

## 3. Document and tool integration

- [x] 3.1 Bind exactly one endpoint per shape with no unlisted/duplicate endpoint. Test shape binding and real app.handle reachability for every path/method/configuration with valid representative params/input; negative omit a binding separately from skipping a mount. A generic404 must not be confused with a modeled domain not_found; fixtures must establish route identity.
- [x] 3.2 Complete documentFromShapes build output and MCP tool derivation, pin operationIds/tool names. Test MCP openapi-tools fixtures against descriptors and emitted document correctness; negatives missing id/inline schema/union arm. Replace committed openapi.json inputs in every build/tool script before removing it from git.
- [x] 3.3 Delete @elysiajs/openapi, obsolete hand-parsed/body-doc/plan-command-schema/query-schema duplication only when no consumers remain. Resolve current absence of hand-parsed-body.ts from TASK-262 rather than recreating/deleting an imaginary file. Test import/build checks and full endpoint/refusal suites; retained domain validation is not deleted merely because it formerly lived beside parsing.

## 4. Frontend shared client — waits for R1

- [x] 4.1 TDD clientFromShapes with fetch/in-process transport, status-specific success and refusal validation, typed client boundary failures. Negatives: backend-only changed known field type, malformed429/503/501 refusal accepted, success/refusal validation bypassed. Positive additive nested response field still renders with previous client validator; strictness belongs to request declarations.
- [x] 4.2 Replace api.ts/wbs-api.ts/saved-plan-api.ts and two hand-written fakes with shape-derived transport/fake; convert screen error branches to Refusal switches. Type negative: rename a response field in contracts and observe fe-01 error at the actual consuming screen. No backend/core handler import enters frontend contracts.
- [x] 4.3 Carry R1 generation/pending-scope tests unchanged in behavior: hold old response across an edit and require trailing read; overlap full/step/tree and require every pending resource install. Negative reintroduce URL-only sharing and lost pending scope separately. New generic transport does not own invalidation policy.
- [x] 4.4 Run complete frontend unit/browser wire regression suites with isolated real servers. Record intentional strict-body/envelope assertion changes individually; preserve unrelated UI/focus and refresh assertions.

## 5. Infrastructure endpoints and completion

- [x] 5.1 Move health and metrics into shapes, text metrics with explicit content type and dependency503; test real reachability/body type/status. Negative skip either mount or collapse dependency failure into200. Delete observability otel-plugin and retag library ring:adapter with exact callers updated.
- [x] 5.2 Delete callerGuard and inline app onRequest only after complete policy coverage; test no route loses auth/origin on production app.handle and endpoint literal tests work without importing Elysia. No exported second-adapter kit is added.
- [x] 5.3 Recheck typecheck timing against pre-generic baseline and apply approved fallback if doubled. Run parent-coordinated frozen workspace format/test/lint/typecheck/build, OpenSpec validation and isolated full browser gate. Verify reports actual output/counts, all fault/restoration results, intentional wire changes and any named omissions before review/commit/archive.

## Adapter preservation hooks — approved refinement

- [x] H1 Write actual-bind type negatives forbidding successful classifier/prevalidation results and undeclared refusal status/body combinations; write mounted contextual-command and callback HEAD/duplicate-query precedence negatives before implementation.
- [x] H2 Add optional backend binding hooks and erased-table forms; run metadata prevalidation after policies and before all request validation, and classify failed request parts without admitting/replacing input. Preserve default validation behavior.
- [x] H3 Watch production-mounted faults for omitted/late prevalidation, skipped classifier, invalid classifier reply validation, and widened type contracts. Restore faults before adjacent Proof comments and record exact observed output.
- [x] H4 Run scoped endpoint/adapter tests, bounded actual-dependency typechecks, lint and formatting; hold for independent review. Production family classifiers and callback migration remain separate slices.
