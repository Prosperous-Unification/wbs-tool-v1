## 1. Prerequisites and authoritative readers

- [ ] 1.1 Start after core-lib-extraction and plan-command-registry; confirm batchServices consumes the UnitOfWork-supplied Scope. Read each mutator and caller named in design.md's matrix, including direct SubtreeStore and DirectoryStore writes. In `libs/core/src/service/working-plan.test.ts` pin batch tests for create→estimate, estimate→child hand-down, dependency→delete survivor, and directory cascade→patch. Compare with uncached admitted stores before adding caching.
- [ ] 1.2 Add `WorkItemStore.listByIds(projectId, ids)` and the five `listByWorkItems(projectId, ids)` readers with design.md's exact typed signatures in core ports, store-sqlite and store-memory. Add source-kit cases for ordered labelled rows, all four step-value collections, incident edges, removed/missing ids and empty input. Negative: omit a label join or return an edge touching neither requested endpoint; invoke the production batch path through that source and assert the exact labelled field/edge before the next command. Run `bunx nx run-many -t test typecheck -p core store-sqlite store-memory conformance`.
- [ ] 1.3 Ensure targeted readers do not hide malformed stored values or cross-project rows. Test production source readers against a deliberately broken reference and an id from another project. Negative: drop the project constraint when integrating refreshRows; assert the working plan never contains the other project's row. Existing missing-reference store proofs stay green.

## 2. Working collection lifecycle

- [ ] 2.1 Implement `createWorkingPlan(scope: Scope<PlanTransactionalStores>, projectId: string): WorkingPlan` with lazy retained collections and detached reads in `libs/core/src/service/working-plan.ts`; use only scope.stores, return explicitly composed PlanTransactionalStores, and close in the runner's finally. Compile a valid accountless scope and assert `workingPlan.stores.users` is a type error; do not use a cast to preserve accounts the runner never consumes. Test `working-plan.test.ts > throws after its batch closes` through a retained callback captured from an actual run; negative: remove closed guard, invoke after settlement, assert rejection at the read rather than permitting stale access.
- [ ] 2.2 Keep every returned before-image detached, including own label arrays and external refs. Test `plan-commands.test.ts > two patches undo to the value before the batch` with two distinct tag sets and names. Negative: mutate a previously returned cached row/array in place; after batch undo read the database and assert the original values, not the most recent cached values.
- [ ] 2.3 Wrap work-item insert/patch/move/freeze/remove in `working-plan-rows.ts` with refreshRows after success. Test every matrix row through runner commands, with dense sibling positions forcing respacing. Negative: omit respaced sibling refresh; a later command's placement/undo precondition must fail on that sibling. Negative: advance the map after a refused patch; next read within the batch must still show the unchanged store value.
- [ ] 2.4 Wrap estimates/actuals/progress/measures in `working-plan-values.ts`, including moveAll source/destination and every metric. Test estimate→first-child→edit-child and delete-last-child hand-up for all four collections. Negative: refresh only destination of moveAll; assert the source has no remaining value during the SAME command's subsequent read, before a later barrier could conceal it.
- [ ] 2.5 Wrap dependency operations in `working-plan-edges.ts`, capturing surviving endpoints before removeAllFor. Test dependency add→reverse cycle refusal and branch delete→surviving endpoint revision. Negative: omit the newly added edge from retained reads; second reversed edge must refuse cycle through the mounted batch. Negative: omit survivor refresh; assert its exact stored revision/precondition differs before the next command.
- [ ] 2.6 Wrap SubtreeStore.insertSubtree including respaced/reparented rows, removed satellite keys and edges. Test duplicate→patch-descendant and restore-related collection controls against uncached stores. Negative: omit removed estimate refresh; assert the prior parent carries no estimate immediately after insertion, before another write.
- [ ] 2.7 Wrap every global DirectoryStore entry/membership mutator with a successful-write reload barrier, while directory reads stay delegated. Wrap the project-scoped `assign` separately: target-refresh only its workItemId on `{ ok:true }`. Test createPerson→setAssignee, setAssignee→patch and cascade-delete-team→patch in the same batch. Negative: remove reload barrier for deletion; assert the second command observes removed labels and updated revision, not merely final state after the batch. Negative: omit assign's target refresh; assert its current stored revision at the next patch/journal precondition. False/refused directory or assignment outcomes do not invent advancement.

## 3. Batch wiring and performance

- [ ] 3.1 Build the working service graph inside UnitOfWork.run(scope), preserving collector ownership and rebuilding a nonworking graph for after-commit announcement. Test `plan-commands.test.ts > refuses then runs another batch without leaked values` and `ordinary write between batches is the next before-image` against both sources. Negative: reuse the previous working graph; assert the second batch's undo restores the intervening value.
- [ ] 3.2 Count full-project reads separately from indexed reads and write statements in `working-plan-performance.test.ts` through a real PlanCommandRunner over SQLite. On 200 distinct rows with distinct prior estimates, assert at most one full read per loaded retained collection; then undo and assert ALL original estimates. Negative: delegate cached listByProject directly; assert the full-read count grows past one. On a frozen checkout/workload/host collect twenty paired warm uncached/cached samples for each homogeneous and mixed fixture, alternating uncached-first/cached-first by pair index and restoring identical authored state outside timing. Retain all samples; report each mode's median and range. Assert cached median <=1.10 times uncached median for EACH fixture independently; ten percent is the authorized tolerance, not an observed performance claim. A failing ratio remains failed acceptance; never delete samples or tick completion because correctness alone passes.
- [ ] 3.2a Include repeated existing-person setAssignee commands in the mixed plan-only fixture. Negative: send `DirectoryStore.assign` through the global-directory full-reload barrier; assert the actual runner's retained-collection full-read count exceeds one. Keep assignment read counts separate because assignments are deliberately not cached.
- [ ] 3.3 Run source conformance, complete core/be-01 unit suites and `bunx nx run-many -t lint typecheck -p core store-sqlite store-memory conformance be-01`, then the full workspace gate (`bin/h2puni-gate.sh` on h2puni) and `OPENSPEC_TELEMETRY=0 openspec validate --all --json`. Write actual verify.md fault table/commands/results only after execution; leave performance shortfalls explicit rather than weakening the one-bulk-read contract.

All failures and performance figures above are future acceptance work, not observations.
