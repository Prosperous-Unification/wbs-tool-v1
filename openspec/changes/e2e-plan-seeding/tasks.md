## 1. Fixture boundary and isolation

- [ ] 1.1 Start from completed measured-rendering (#353, f66f73e8). Record the current commit and map setup versus tested gesture in `rendering-fixture.ts` and `plan-surface.spec.ts`. Preserve logical aria-rowcount readiness, renderingGeometry and existing acceptance/fault proofs from rendering-baseline.spec.ts; retained hashed measurements stay historical after harness changes. Keep nonallowlisted UI seeds unchanged. Tests `create-project` consumers continue observing rename arming; run `bunx nx run fe-01:typecheck` after any fixture type edit.
- [ ] 1.2 Implement typed recipe compilation, chunks capped at 200, cross-chunk ref resolution and unique names in `e2e/plan-fixture.ts`. Test `plan-fixture.spec.ts > resolves a predecessor in an earlier chunk` using 201 rows. Negative: leave an earlier ref unresolved; the real backend must refuse the exact later command, before any geometry sample. Duplicate-recipe-ref negative must fail before any write request.
- [ ] 1.3 Replace renderingRequest's unconstrained generic cast with a generated-shape transport over Page same-origin fetch and shared response validation. Browser tests `setup refuses a real backend refusal`, `setup refuses a missing row id`. Negative: remove response validation or the index/ref/id check; assert setup rejects at the first malformed response, not a later setEstimate error. Use the real public route with an intercepted malformed response only for that boundary fault.
- [ ] 1.4 Verify seeded server state independently from recipe expectations in `plan-fixture.spec.ts`. Negative: omit one estimate request while returning 200 from other requests; assert the exact row/step's stored estimate before mounting the tested table. Negative: drop one directory link; assert that id on its named row. No expected values derived from the returned tree being tested.

## 2. Selected adoption

- [ ] 2.1 Migrate `rendering-fixture.ts` to the shared helper, preserving off-table bulk writes, final state verification and timing boundaries. Run `rendering-fixture.spec.ts` and rerun its documented malformed-id and zero-geometry mutations. Fault sensitivity must remain at the same setup/layout stage.
- [ ] 2.2 Convert only static prerequisites in `plan-surface.spec.ts`. Keep actual UI create/typing where a case tests armed focus or focus after a write. Test existing short/tall-plan geometry and keyboard-navigation cases. Negative: re-inject each converted scenario's own production fault; if a case no longer fails, restore that case's UI seed rather than counting the conversion as complete.
- [ ] 2.3 Test two simultaneous recipes with identical logical labels but unique run/worker/test prefixes. Assert returned ids, selected project and directory refs for each. Negative: remove test/worker identity from generated names and deliberately overlap saves; assert the two fixtures share neither ids nor names. Assert exact fixture identities, not global directory counts.

## 3. Measured concurrency and final evidence

- [ ] 3.1 Audit global listing assertions across all e2e specs before a four-worker run. Make selected fixtures' assertions identity-specific; retain an explicit serial lane for a spec whose requirement is a global count. Negative for any added isolation guard: make two fixtures share that asserted target and watch the actual spec fail.
- [ ] 3.2 Run the full browser suite three times with one worker and three times with four, zero retries, frozen checkout, fresh database per run and owned safe ports. Use `CI=1 E2E_PORT_SHIFT=1900 bun run e2e --workers=1` and `--workers=4` only after checking all three shifted ports; assign another known-safe noncolliding shift if occupied. Never reuse another checkout's server or stop it. Record full wall times and lock/busy failures.
- [ ] 3.3 Apply design.md's acceptance rule: all six runs green, zero lock failures and four-worker median at least 20% faster → change workers to four; otherwise retain one and record the measured refusal. Test `playwright-config.test.ts` for any config changes and add any new root-level helper to the explicit lint target.
- [ ] 3.4 Run `bunx nx run-many -t test lint typecheck -p fe-01 contracts`, full workspace gate (`bin/h2puni-gate.sh` on h2puni) and `OPENSPEC_TELEMETRY=0 openspec validate --all --json`. Record actual performance, fault output and final full-suite results in verify.md; no prefilled success table.

The design pass started no servers and ran no browser measurements.
