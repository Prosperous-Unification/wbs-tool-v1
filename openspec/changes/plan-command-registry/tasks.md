## 1. Characterization and contracts

- [ ] 1.1 Rebase after core-lib-extraction; map the current work-item binder, parser and runner to the paths in design.md, and pin all 36 kinds in an independent fixture in `libs/contracts/src/commands/definitions.test.ts`. Preserve mounted controls in `apps/be-01/src/http/elysia/work-item.test.ts` for malformed nested extras, absent/null values, semantic invalid values and 201-command precedence. Run `bunx nx run-many -t test typecheck -p contracts core be-01 mcp-01`; record baseline without changing production.
- [ ] 1.2 Add commandDefinitions/defineCommand and derive PlanCommandWire, PlanCommandKind and PLAN_COMMAND_KINDS. Convert `http/plan-command-shapes.ts` and `http/refusal.ts` to consume them. Test `definitions.test.ts > definition key agrees with its discriminator`; type-negative: bind clearMeasure under setMeasure and require a compile error. Runtime negative: remove clearMeasure definition, then generated MCP input must fail its independently pinned kind-set assertion.
- [ ] 1.3 Carry structural descriptors unchanged through `http/plan-command-shapes.test.ts` and `apps/mcp-01/src/shape-document.test.ts`. Negative: substitute a duplicate kind while preserving total arms; at tool generation assert the missing and duplicate kind names. Negative: empty createWorkItem description; assert that kind's own description length, not any branch's. Execute the actual tool generator, not only a registry helper.

## 2. Semantic normalization and execution

- [ ] 2.1 Extract typed commandNormalizers to `libs/core/src/service/command-normalizers.ts`, infer normalized PlanCommand from their returns and delete the handwritten normalized union. Test `command-normalizers.test.ts > preserves priority absence null and number` and `defaults missing assignee to null`. Negative: default absent priority to null, send through the mounted command route and assert the created row loses its independently configured middle-band priority.
- [ ] 2.2 Require each structural definition's typed normalizer; retain field parsers/refusal translation and parse-all-before-cap order. Compile-negative: add temporary definition with no normalizer; expect error at normalizer record. Mounted negative: move cap before semantic parsing; send invalid semantic field at index 200 of 201 commands, assert the exact field refusal/index rather than too_many_commands. No production Proof comment until failure is observed.
- [ ] 2.3 Introduce CommandContext, CommandFor/AppliedFor and `bindCommands(graph): CommandBindings` in `libs/core/src/service/command-bindings.ts` with design.md's kind-indexed input/output intersection types; move handlers out of the runner while leaving collector/uow/publication ownership there. Test `plan-commands.test.ts > creates by ref then estimates the minted row`, directory-only project_required and duplicate_ref ordering. Negative: make setEstimate target the previous raw id instead of ref resolution; assert the persisted named row's exact estimate through a store read.
- [ ] 2.4 Close binding completeness and dispatch type correlation. Compile valid plain-kind and entity-kind bindings first, then compile-negatives in `command-bindings.test.ts`: omitted binding, wrong command input and wrong response kind (a setEstimate binding returning clearEstimate, plus createPerson returning createTeam). If a single indexed-dispatch cast is required, place its boundary comment beside it; widening `Promise<AppliedFor<K>>` to `Promise<AppliedCommand>` must turn the wrong-response @ts-expect-error fixture into TS2578. Negative: add one binding under the wrong kind; invoke the actual runner and assert the command-specific store effect, not handler count.
- [ ] 2.5 Collapse only type-compatible directory triples, retaining person/team special branches. Test existing directory batch and rollback cases unchanged. Negative: route createWorkItemType to createTag; run the directory command HTTP endpoint, assert the new name exists only in work-item types.

## 3. Handoff and gates

- [ ] 3.1 Remove obsolete EVERY_KIND, DIRECTORY_KINDS and duplicate kind unions only after callers use definitions; check no service/storage runtime import enters contracts with the existing ring gate. Negative: inject a core import into definitions and watch lint reject that path.
- [ ] 3.2 Run `bunx nx run-many -t test lint typecheck -p contracts core be-01 fe-01 mcp-01`; run the full workspace gate on the final tree (`bin/h2puni-gate.sh` on h2puni), plus `OPENSPEC_TELEMETRY=0 openspec validate --all --json`. Re-run generated tools and existing API/browser command coverage at that tree. Write verify.md only with actual commands/results, per-fault failures and restored greens; report skipped checks explicitly.

All tests and faults above are planned. None is evidence from this design pass.
