## 1. Characterize existing security and capacity behavior

- [ ] 1.1 Run `bunx nx run mcp-01:test` and `bunx nx run auth:test`; pin existing registration, per-source promotion reservation, global/per-client pending, grant and session-capacity cases in `apps/mcp-01/src/oauth.test.ts`. Read the actual current case count; do not inherit historical 24 as a target.
- [ ] 1.2 Add `wrong state preserves the honest MCP login and browser cookie`: authorize through InMemoryMcpOAuth.response, apply a wrong-state callback's Set-Cookie to a cookie jar, then send the honest callback. It must initially fail against delete-before-compare. After implementation, reinsert premature deletion and separately reinsert cookie clearing; each must fail at honest callback status/exchange count, not merely at map size.
- [ ] 1.3 Add expiry-before-mismatch and matched-state replay controls through the same HTTP handler. Negative: compare before expiry or retain after match; assert expired metadata/cookie removal and exactly one exchange for two matching callbacks.

## 2. Shared store plus bounded metadata

- [ ] 2.1 Export digestOidcBinding from auth and remove browserBinding from private stored record values. Test real MCP authorize via the adopted store and inspect its actual backing maps in tests only. Separate negatives: key raw binding again; spread browserBinding into the value again. At the saved-login instant, assert no retained key/value contains the generated binding. Do not claim HTTP secrecy tests can see private retention.
- [ ] 2.2 Implement PendingAuthorizations in `apps/mcp-01/src/pending-authorizations.ts` with synchronous shared-store/context save, no-eviction admission and one clock/TTL. Tests through real authorize: `refuses authorization at capacity without evicting an in-flight login`, `partitions pending authorization capacity by client`, and expiry frees both counts. Negative: omit per-client check; assert another request by the same client is 429 while another client's request remains 302. Negative: evict at capacity; honest first callback must fail and expose the regression.
- [ ] 2.3 Guard impossible generated-binding collision and consumed-proof/missing-context. Tests `duplicate live binding cannot overwrite an existing authorization` with deterministic random, and `consumed proof with missing metadata throws before exchange` after deleting only the context map entry. Negative for each: remove its guard; assert first authorization context remains correct, or assert rejection before exchange/grant issuance.
- [ ] 2.4 Wire oauth.ts authorize/callback/cleanup to the wrapper and delete its raw Transaction map. Forward matched arriving state to upstream.exchange; downstream state still comes from AuthorizationContext. Test downstream callback URI, PKCE challenge, scopes and state remain exact. Negative: confuse upstream and downstream state; assert the redirect's state equals the caller's distinct original state.
- [ ] 2.5 Implement mismatch-preserving callback cookie behavior with existing error status and no provider call. Run the negatives from 1.2, expiry and replay, and the entire existing capacity suite. Grant-cap refusal after matched consumption still spends the proof, matching current behavior.

## 3. Primitive proof and integration gates

- [ ] 3.1 Add an isolated Bun test process instrumenting node:crypto timingSafeEqual, and execute honest/wrong-state MCP callbacks through the real wrapper/store. Assert the primitive sees equal-length digest bytes. Negative: replace shared sameSecret with direct equality; the primitive-call assertion must fail while the normal wrong-state rejection is the control. Record instrumentation availability honestly; elapsed-time sampling is not an acceptable substitute.
- [ ] 3.2 Run `bunx nx run-many -t test lint typecheck -p auth mcp-01 be-01`, including be-01's existing OIDC integration and plural-binding cases unchanged. Then full workspace gate (`bin/h2puni-gate.sh` on h2puni) and `OPENSPEC_TELEMETRY=0 openspec validate --all --json`. Record actual watched failures and restored green outputs in verify.md; add Proof comments only from observed output.

This packet makes no claim that timing instrumentation or any security negative was executed during design.
