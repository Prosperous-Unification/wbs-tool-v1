## 1. Characterize existing security and capacity behavior

- [x] 1.1 Run `bunx nx run mcp-01:test` and `bunx nx run auth:test`; pin existing registration, per-source promotion reservation, global/per-client pending, grant and session-capacity cases in `apps/mcp-01/src/oauth.test.ts`. Read the actual current case count; do not inherit historical 24 as a target.
  - Evidence: the untouched baseline had 24 OAuth cases and 114 MCP cases; the named registration, promotion-reservation, pending, grant, and session-capacity cases all passed. The auth target passed 95 cases outside the filesystem sandbox after its four localhost-listener cases were unavailable inside it.
- [x] 1.2 Add `wrong state preserves the honest MCP login and browser cookie`: authorize through InMemoryMcpOAuth.response, apply a wrong-state callback's Set-Cookie to a cookie jar, then send the honest callback. It must initially fail against delete-before-compare. After implementation, reinsert premature deletion and separately reinsert cookie clearing; each must fail at honest callback status/exchange count, not merely at map size.
  - Evidence: the route-level browser-cookie test was red first and is green now; both restored premature deletion and restored mismatch cookie clearing made the honest callback return 400 instead of 302 before any exchange-count success claim.
- [x] 1.3 Add expiry-before-mismatch and matched-state replay controls through the same HTTP handler. Negative: compare before expiry or retain after match; assert expired metadata/cookie removal and exactly one exchange for two matching callbacks.
  - Evidence: compare-before-expiry retained one transaction instead of zero; retain-after-match made the exact exchange count two instead of one. The restored tests assert cookie clearing, exact metadata removal, one exchange, and replay refusal.

## 2. Shared store plus bounded metadata

- [x] 2.1 Export digestOidcBinding from auth and remove browserBinding from private stored record values. Test real MCP authorize via the adopted store and inspect its actual backing maps in tests only. Separate negatives: key raw binding again; spread browserBinding into the value again. At the saved-login instant, assert no retained key/value contains the generated binding. Do not claim HTTP secrecy tests can see private retention.
  - Evidence: real authorize retained only the expected digest in both backing-map keys and no generated binding in either map's serialized values; raw-key and raw-value mutations failed those separate assertions.
- [x] 2.2 Implement PendingAuthorizations in `apps/mcp-01/src/pending-authorizations.ts` with synchronous shared-store/context save, no-eviction admission and one clock/TTL. Tests through real authorize: `refuses authorization at capacity without evicting an in-flight login`, `partitions pending authorization capacity by client`, and expiry frees both counts. Negative: omit per-client check; assert another request by the same client is 429 while another client's request remains 302. Negative: evict at capacity; honest first callback must fail and expose the regression.
  - Evidence: one save sampled the source clock once; global/per-client and expiry controls passed. Omitting the client limit admitted a second request, eviction made the first callback fail, and disabling cleanup left the expired capacity unavailable.
- [x] 2.3 Guard impossible generated-binding collision and consumed-proof/missing-context. Tests `duplicate live binding cannot overwrite an existing authorization` with deterministic random, and `consumed proof with missing metadata throws before exchange` after deleting only the context map entry. Negative for each: remove its guard; assert first authorization context remains correct, or assert rejection before exchange/grant issuance.
  - Evidence: removing collision refusal made the original callback return 400 after overwrite. Missing context throws after proof consumption with zero provider exchanges and zero grants; returning `missing` left the required error undefined.
- [x] 2.4 Wire oauth.ts authorize/callback/cleanup to the wrapper and delete its raw Transaction map. Forward matched arriving state to upstream.exchange; downstream state still comes from AuthorizationContext. Test downstream callback URI, PKCE challenge, scopes and state remain exact. Negative: confuse upstream and downstream state; assert the redirect's state equals the caller's distinct original state.
  - Evidence: exact callback URI, challenge, scopes, upstream exchange checks, and distinct downstream state passed. State-confusion mutations produced `random-3` instead of `claude-state` downstream and `claude-state` instead of `random-3` upstream.
- [x] 2.5 Implement mismatch-preserving callback cookie behavior with existing error status and no provider call. Run the negatives from 1.2, expiry and replay, and the entire existing capacity suite. Grant-cap refusal after matched consumption still spends the proof, matching current behavior.
  - Evidence: the adopted-store mismatch deletion, cookie clearing, expiry ordering, and replay-retention mutations all failed route controls. Full MCP passed 124 tests; grant-cap refusal exchanged twice across two distinct logins, then replay returned 400 without a third exchange.

## 3. Primitive proof and integration gates

- [x] 3.1 Add an isolated Bun test process instrumenting node:crypto timingSafeEqual, and execute honest/wrong-state MCP callbacks through the real wrapper/store. Assert the primitive sees equal-length digest bytes. Negative: replace shared sameSecret with direct equality; the primitive-call assertion must fail while the normal wrong-state rejection is the control. Record instrumentation availability honestly; elapsed-time sampling is not an acceptable substitute.
  - Evidence: Bun 1.4.2 preload instrumentation observed two real native calls with 64/64-byte operands through the MCP wrapper/store. Direct equality left the wrong-state/honest callback control green but produced zero primitive calls, failing the exact call assertion.
- [ ] 3.2 Run `bunx nx run-many -t test lint typecheck -p auth mcp-01 be-01`, including be-01's existing OIDC integration and plural-binding cases unchanged. Then full workspace gate (`bin/h2puni-gate.sh` on h2puni) and `OPENSPEC_TELEMETRY=0 openspec validate --all --json`. Record actual watched failures and restored green outputs in verify.md; add Proof comments only from observed output.

This packet makes no claim that timing instrumentation or any security negative was executed during design.
