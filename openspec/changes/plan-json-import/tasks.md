## 1. Current export and document boundary

- [ ] 1.1 Rebase on completed core extraction and scheduler-port contracts; map current project/work-item route tests to core/http and be-01 mounts. Re-read the live export response and authored project/row fields. Test `libs/core/src/service/plan-document.test.ts > preserves the existing JSON export fields` against the current shared response fixture, including deadline and optimizer fields. Record baseline commands and exact commit.
- [ ] 1.2 Add PlanDocument declarations in `libs/contracts/src/http/plan-document-shapes.ts` and additive export construction in core/service/plan-document.ts plus core/http/project.routes.ts. Test `JSON export is a versioned plan document` and `settings says what project says`. Negative: drop one setting such as estimateRounding; mounted JSON-export assertion must fail on that exact field while old projection fields remain controls.
- [ ] 1.3 Build the transitive referenced directory closure with capacity-only teams, people memberships and team services, plus calendar markers. Test `capacity-only team is named`, `assigned agent keeps memberships`, `unreferenced tag is excluded`. Negatives: remove each respective traversal/filter; invoke the actual export endpoint, assert the exact required/excluded name and id. Missing referenced entry negative must throw, not render an unknown-name default.
- [ ] 1.4 Implement archival writable projection and path classifier using existing SchemaShape, requestSchema deletion and Standard Schema issues. Test mounted `malformed priority names workItems[3].priority` and `unknown version precedes version-specific validation`. Negatives: loosen priority or handle version only after full version-1 parsing; assert exact 400 code/path against the document endpoint. No TypeBox, committed OpenAPI or second parser.

## 2. Pure preparation

- [ ] 2.1 Implement PreparedImport/ImportPreparation in `libs/core/src/service/prepare-import.ts`: file-id indexes, unique ids and normalized names, structural values and deterministic first-fault paths. Test duplicate row id, duplicate trimmed tag name, invalid date/priority/band/capacity and unknown step/system/person refs. Negative per guard: remove it and run the mounted import with a counting UnitOfWork; assert typed refusal with zero admissions, not a later foreign-key failure.
- [ ] 2.2 Validate hierarchy and complete dependency graph before admission: parent existence, self/cycle, sibling position uniqueness, dependency endpoints and existing canDepend rules against the entire graph. Test forward references, out-of-order parents and `fully declared parent cycle refuses before admission`. Negative: skip cycle validation; assert the named refusal and zero unit-of-work calls so a later stack overflow cannot masquerade as proof.
- [ ] 2.3 Compute leafhood solely from parentId and parse the four step-value maps only for leaves. Test `parent aggregate maps never become stored facts` with a true parent whose exported rolledUp says false and whose aggregate maps carry deliberately invalid content. Negative: treat exported rolledUp as authority or copy parent maps; after import inspect the actual satellite stores and assert no parent values. Preserve explicit assignees; doesEveryStep is never read as an assignment source.
- [ ] 2.4 Check deadline versus project start and requested scheduler capability through current domain/port refusals. Tests `deadline before day zero is refused`, `unavailable enabled optimizer creates nothing`, `disabled optimized preference is retained`. Negative: silently reset enabled optimization to false; assert typed refusal, zero project creation and zero source writes.

## 3. Atomic source writes

- [ ] 3.1 Implement ImportService over UnitOfWork.run(scope) and compose its collector/services over THAT Scope. Resolve target directory names and solution slug while admitted. Test `existing tag is reused`, `existing person remains unchanged`, `new agent retains kind and memberships` against SQLite and memory sources. Negative: replace existing memberships with imported ones; assert the global existing person remains byte-equivalent on those fields.
- [ ] 3.2 Create the exact supplied project settings and ordered steps through ProjectStore.create, avoiding default Dev/QA insertion. Add bands/capacity and calendar markers. Test `three nondefault steps retain their order` and `marker date name color restored`. Negative: use ProjectService.create defaults; read the created project's steps and assert only the declared names in their order.
- [ ] 3.3 Mint fresh ids and write one SubtreeCopy, then own label sets and ordered external refs through scoped stores. Test row hierarchy, frozenNumber, deadline/not-before/reason, priority/maxParallel, leaf estimates/actuals/progress/measures, explicit assignments and dependencies. Negative: omit notes from copy; read the new tree's exact row notes. Negative: copy source ids; assert every imported entity id differs from every corresponding file id and original rows stay unchanged.
- [ ] 3.4 Prove real rollback after writes have occurred. Use a VALID document with an absent team, hold a later scoped store write, first assert the created team is visible through the admitted scope, then inject a modeled refusal and separately a thrown source error. After terminal settlement assert no team/project/rows and no publications. Negative: force commit:true for the modeled refusal or bypass the real UnitOfWork rollback; assert the created team leaks. The old dangling-dependency fixture is PRE-admission and cannot prove rollback.
- [ ] 3.5 Keep/free/taken solution refs through the admitted name lookup. Test concurrent imports of the same free slug: one kept, one left-off, both valid projects. Negative: check slug outside admission or omit check; assert two successful imports with exact distinct outcomes, not a leaked uniqueness exception.
- [ ] 3.6 Publish collected directory/project/tree events after successful commit and gate release; no undo/history rows. Test a held publisher while another ordinary write completes and a thrown/refused import emits nothing. Negative: publish inside the unit of work; assert the queued ordinary write completes while publish is still held. Negative: omit directory announcement; an existing subscriber's directory refresh must receive the imported name.
- [ ] 3.7 Round-trip export→import→export on a compatible directory and compare authored input modulo ids/stamps/solution-ref outcome. Include a parent and leaf, nondefault steps, all satellite kinds, deadline, markers and optimization settings; do not compare computed optimization cache generations or seq. Negative: restore parent aggregate instead of leaf-only values; assert the stored source tables and re-exported author values.

## 4. Mounted HTTP, generated clients and UI

- [ ] 4.1 Add the postApiProjectsImport shape and bind/mount it with cookie-origin and write-scope policies, typed 201 summary and 400/403/409 refusals. Test shape/endpoint bijection and mounted auth/origin precedence. Negatives: skip mount → actual path 404; remove write policy → read-only token writes; move policy after parsing → malformed read-only request reports invalid_body instead of insufficient_scope.
- [ ] 4.2 Verify generated OpenAPI/MCP input has the import operation and inline object schema; assert the operation by id rather than a hardcoded count of 23. Test `apps/mcp-01/src/openapi-tools.test.ts > exposes the import document tool`. Negative: omit import from shared shapes; assert the actual generated tool is missing. Response type-negative must reject incomplete created summaries.
- [ ] 4.3 Add the narrow ProjectApi facade methods backed by generated client, menu text, Download JSON and hidden file input. Test production WbsTable `download includes collapsed and filtered-out rows`. Negative: serialize visibleRows; inspect downloaded JSON and assert the hidden row's exact id/name.
- [ ] 4.4 Wire success to the real picker onOpenProject path and one summary toast; cover cancellation, invalid JSON, read failure, request refusal, repeated same-file selection and in-flight submit blocking. Negative: open project in refusal path; assert no open callback and the previous project remains selected. Negative: leave file input value set; choose the same file twice and assert two separately completed imports.
- [ ] 4.5 Add `apps/fe-01/e2e/plan-import.spec.ts`: real Download JSON, real file chooser import, new project selection, summary, and dangling-ref refusal with project count unchanged. For the refusal negative bypass pure ref validation; assert the precise path/code toast so a generic 500 cannot pass the case. Own ports and database; do not reuse a running checkout.

## 5. Measurement and final evidence

- [ ] 5.1 Measure a 500-row document's preparation and admitted lock time separately; report input size, statement counts and queued ordinary-write completion. No speed target is invented; unusually long hold is explicit evidence, not a reason to skip atomicity.
- [ ] 5.2 Run `bunx nx run-many -t test lint typecheck -p contracts core store-sqlite store-memory conformance be-01 fe-01 mcp-01`, the full workspace gate (`bin/h2puni-gate.sh` on h2puni), whole browser gate on owned ports, and `OPENSPEC_TELEMETRY=0 openspec validate --all --json`. Replace the preexisting unimplemented verify placeholder with actual observed fault/green evidence and exact commands; never tick tasks from artifact presence alone.

All slices remain unchecked. This readiness revision implements no import behavior and records no runtime passes.
